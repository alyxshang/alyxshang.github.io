<h2 class="sectionHead">{{ site.navHeading }}</h2>
<ul class="nav">
 {% for item in data["nav"] %}
  <li class="nav"><a class="nav" href="{{ site.baseurl }}{{ item.link }}">{{ item.name }}</a></li>
 {% endfor %}
</ul>